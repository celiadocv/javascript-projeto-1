function setup() {
  createCanvas(400, 400);
  background("violet");
}

function draw() {
  fill("purple");
  stroke("rgb(225,225,225) ")
  if(mouseIsPressed);
  circle(mouseX, mouseY, 30, 50 );
  
}
